// Placeholder shell — real screens land in T-07+ (see .claude/plans/PLAN.md).
export default function App() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "grid",
        placeItems: "center",
        background: "#0e0f10",
        color: "rgba(255,255,255,0.4)",
        fontFamily: "Inter, system-ui, sans-serif",
        letterSpacing: "2px",
        textTransform: "uppercase",
        fontSize: 12,
      }}
    >
      Focus
    </main>
  );
}
