import { Placeholder } from "../focus/FocusScreen";

// Placeholder — streak/heatmap/bars/donut land in T-12.
export function InsightsScreen() {
  return <Placeholder label="Insights" note="Streak, heatmap, bars, donut — T-12" />;
}
